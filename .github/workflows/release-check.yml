name: Release Check

on:
  push:
    branches: [ main, master ]
  pull_request:

jobs:
  skill-pack-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Assert repository hygiene
        shell: bash
        run: |
          set -euo pipefail
          if find . -name '.DS_Store' | grep -q .; then
            echo 'Found .DS_Store files in repository'
            find . -name '.DS_Store'
            exit 1
          fi

      - name: Syntax check helper scripts
        shell: bash
        run: |
          set -euo pipefail
          test "$(find skills -maxdepth 2 -name SKILL.md | wc -l | tr -d ' ')" = "1"
          test -f skills/memobank/SKILL.md
          node scripts/vendor-shared.mjs
          git diff --exit-code
          node -c skills/_shared/scripts/init-mb.js
          node --check skills/mb-garden/assets/mb-lint.mjs

      - name: Local package install smoke test
        shell: bash
        run: |
          set -euo pipefail
          TMP_DIR="$(mktemp -d)"
          trap 'rm -rf "$TMP_DIR"' EXIT

          mkdir -p "$TMP_DIR/project"
          (
            cd "$TMP_DIR/project"
            npx -y skills add "$GITHUB_WORKSPACE" --skill memobank --yes

            test -f .agents/skills/memobank/SKILL.md
            test -f .agents/skills/memobank/references/shared-structure-template.md
            test -f .agents/skills/memobank/agents/shared-repo-scanner.md
            test -f .agents/skills/memobank/scripts/shared-init-mb.js

            mkdir -p target-repo
            cd target-repo
            node ../.agents/skills/memobank/scripts/shared-init-mb.js --preset greenfield

            test -f .memory-bank/commands/prd.md
            test -f .memory-bank/commands/execute.md
            test -f .claude/skills/prd/SKILL.md
            test ! -e .memory-bank/commands/map-codebase.md
            test ! -e .agents/skills/autonomous/SKILL.md
            test ! -e .claude/skills/map-codebase/SKILL.md
            node ../.agents/skills/memobank/scripts/shared-init-mb.js --sync --commands execute,verify,review,mb-sync

            test -f .memory-bank/commands/execute.md
            test -f .memory-bank/commands/verify.md
            test -f .memory-bank/commands/review.md
            test -f .memory-bank/commands/mb-sync.md
            test ! -e .memory-bank/commands/prd.md
            test ! -e .claude/skills/prd/SKILL.md
            test -f AGENTS.md
          )

      - name: Dry bootstrap + sync smoke test
        shell: bash
        run: |
          set -euo pipefail
          TMP_DIR="$(mktemp -d)"
          trap 'rm -rf "$TMP_DIR"' EXIT

          (
            cd "$TMP_DIR"
            node "$GITHUB_WORKSPACE/skills/_shared/scripts/init-mb.js"
            node "$GITHUB_WORKSPACE/skills/_shared/scripts/init-mb.js" --sync

            test -f .memory-bank/commands/autonomous.md
            test -f .memory-bank/workflows/autonomy-policy.md
            test -f .claude/skills/cold-start/SKILL.md
            test -f .claude/skills/autonomous/SKILL.md
            test -f .agents/skills/cold-start/SKILL.md
            test -f .agents/skills/autonomous/SKILL.md

            mkdir -p scripts
            cp "$GITHUB_WORKSPACE/skills/mb-garden/assets/mb-lint.mjs" scripts/mb-lint.mjs
            node scripts/mb-lint.mjs
          )
